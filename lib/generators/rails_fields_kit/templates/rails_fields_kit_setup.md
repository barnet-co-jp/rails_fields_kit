# Rails Fields Kit setup

Generated by `rails generate rails_fields_kit:install`. Use as a quick checklist for wiring Rails Fields Kit into this app.

## Setup checklist

- [ ] Run `rails rails_fields_kit:doctor` to check detectable setup state.
- [ ] Confirm `config/initializers/rails_fields_kit.rb` matches this app's needs.
- [ ] Install Tom Select: `yarn add tom-select` (or npm/pnpm).
- [ ] Register the Stimulus controller on this app's Stimulus application:
  ```js
  import { TomSelectController } from "rails_fields_kit"
  application.register("rails-fields-kit--tom-select", TomSelectController)
  ```
- [ ] Load Tom Select CSS: `import "tom-select/dist/css/tom-select.css"`
- [ ] Render a first `rfk_select` field with a server-rendered collection.
- [ ] If using importmap: run `rails generate rails_fields_kit:install --importmap` or add pins manually.
- [ ] If using Vite/bundler: add resolve aliases for `rails_fields_kit` (see README).

## Use documentation that matches the installed gem

Do not use the repository `main` branch as the API specification for a host application pinned to another version.

Resolve the installed gem first:

```bash
bundle show rails_fields_kit
```

Read `README.md` and `doc/*.md` from that directory. If you inspect the private GitHub repository with `gh`, use the exact tag or commit resolved by this application's `Gemfile.lock`.

Start with:

- `doc/host_app_integration.md` for host-app and AI/agent usage rules.
- `doc/field_helpers.md` for helper selection and field semantics.
- `doc/controller_helpers.md` for endpoint options.
- `doc/public_api.md` for the supported public surface.
- `doc/events.md` for Stimulus events.

## Next steps

- Add remote endpoints with `rfk_combobox` + `rfk_search_with` / `rfk_find_with` / `rfk_create_with` when the selected remote value is the submitted identity.
- Use `rfk_lookup` when free text and an optional selected master ID must remain separate.
- Keep host-specific query execution, authorization, and endpoint scope in this application rather than redefining Rails Fields Kit option semantics.

## App-specific notes

- Stimulus entrypoint:
- Tom Select CSS loaded from:
- First field:
